# AGENTS.md — Host Provisioning Reference

[`INSTALL.md`](INSTALL.md) is the provisioning plan for bootstrapping a Linux x86_64 host. Read it and execute each section declaratively — the file states *what* must be in place; you determine *how* at runtime.
